# KFP v2 IR YAML (YAML-only template)
pipelineInfo:
  name: rollout-traffic-split
root:
  dag:
    tasks:
      rollout:
        cachingOptions:
          enableCache: false
        componentRef:
          name: comp-rollout-split
        inputs:
          parameters:
            deployment_base_url:
              componentInputParameter: deployment_base_url
            tenant_id:
              componentInputParameter: tenant_id
            project_id:
              componentInputParameter: project_id
            user_id:
              componentInputParameter: user_id
            deployment_id:
              componentInputParameter: deployment_id
            model_version_id:
              componentInputParameter: model_version_id
            canary_percent:
              componentInputParameter: canary_percent
        taskInfo:
          name: rollout
inputDefinitions:
  parameters:
    deployment_base_url:
      parameterType: STRING
    tenant_id:
      parameterType: STRING
    project_id:
      parameterType: STRING
    user_id:
      parameterType: STRING
    deployment_id:
      parameterType: STRING
    model_version_id:
      parameterType: STRING
    canary_percent:
      parameterType: NUMBER_INTEGER
components:
  comp-rollout-split:
    executorLabel: exec-rollout-split
    inputDefinitions:
      parameters:
        deployment_base_url:
          parameterType: STRING
        tenant_id:
          parameterType: STRING
        project_id:
          parameterType: STRING
        user_id:
          parameterType: STRING
        deployment_id:
          parameterType: STRING
        model_version_id:
          parameterType: STRING
        canary_percent:
          parameterType: NUMBER_INTEGER
deploymentSpec:
  executors:
    exec-rollout-split:
      container:
        image: docker.io/library/python:3.11-slim
        command:
          - sh
          - -ec
        args:
          - |
            python - <<'PY'
            import json
            import urllib.request
            import urllib.error

            base = "{{$.inputs.parameters['deployment_base_url']}}".rstrip('/')
            tenant_id = "{{$.inputs.parameters['tenant_id']}}"
            project_id = "{{$.inputs.parameters['project_id']}}"
            user_id = "{{$.inputs.parameters['user_id']}}"
            deployment_id = "{{$.inputs.parameters['deployment_id']}}".strip()
            model_version_id = "{{$.inputs.parameters['model_version_id']}}".strip()
            canary_percent = int("{{$.inputs.parameters['canary_percent']}}")

            if not deployment_id:
              raise SystemExit('deployment_id is required')

            headers = {
              'Content-Type': 'application/json',
              'X-Tenant-Id': tenant_id,
              'X-Project-Id': project_id,
              'X-User-Id': user_id,
            }

            payload = {
              'traffic': {'canaryTrafficPercent': canary_percent}
            }
            if model_version_id:
              payload['model_version_id'] = model_version_id

            data = json.dumps(payload).encode('utf-8')
            req = urllib.request.Request(
              url=f"{base}/api/v1/deployments/{deployment_id}",
              data=data,
              headers=headers,
              method='PUT'
            )

            try:
              with urllib.request.urlopen(req, timeout=20) as resp:
                out = resp.read().decode('utf-8')
                print(out)
            except urllib.error.HTTPError as e:
              out = e.read().decode('utf-8')
              raise SystemExit(f"rollout_failed status={e.code} body={out}")
            PY
schemaVersion: 2.1.0
sdkVersion: kfp-2.15.0
