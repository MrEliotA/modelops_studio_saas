apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  # Secrets are environment-specific; create them via your secret manager (recommended)
  # or apply deploy/k8s/feast/secrets.example.yaml (not referenced by default).
  - feature-repos
