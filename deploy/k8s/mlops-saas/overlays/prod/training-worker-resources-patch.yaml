apiVersion: apps/v1
kind: Deployment
metadata:
  name: training-worker
  namespace: mlops-system
spec:
  template:
    spec:
      containers:
        - name: worker
          resources:
            requests:
              cpu: "500m"
              memory: "512Mi"
            limits:
              cpu: "4"
              memory: "2Gi"
