apiVersion: v1
kind: Secret
metadata:
  name: mlops-secrets
  namespace: mlops-system
type: Opaque
stringData:
  # 👉 set these to your production endpoints / credentials.
  # ❗ Do not commit real credentials. Prefer your secret manager / ExternalSecrets.
  DATABASE_URL: "REPLACE_ME"
  AWS_ACCESS_KEY_ID: "REPLACE_ME"
  AWS_SECRET_ACCESS_KEY: "REPLACE_ME"